{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "issue-reviewer-return.schema.json",
  "title": "issue-reviewer-return",
  "description": "issue reviewer(judge)が orchestrator(`commands/harness-orchestrate.md`「issue reviewer」節)へ返す JSON の形を規定する。PR フェーズの `reviewer-return.schema.json` を issue フェーズへ対称に写したもの。issue reviewer 自身は台帳(`.harness/plan-progress.json`)・ラベルへ直接書き込まず、この形の値を返すだけに留める(doer≠judge・単一書込主体は orchestrator)。orchestrator はこの値を検証してから issue.status の遷移・ラベル同期・reports[] の代筆を行う。判定確定時は has_blocker/blocker_count/escalate/review_markdown の 4 フィールドを返し、レビュー中に人間の判断が必要と感じた場合は、これに加えて escalate_to_human を返してよい(`rules/escalate-to-human.md` 準拠。他フィールドとの共存可 — 客観的な escalate(round/blocker trend の停止条件)とは独立のシグナルで排他ではない)。この形は skill 出力(`reviewing-github-issues` が返す `{review_markdown, has_blocker, recommended_label?}`)の superset であり、dispatch wrapper(`roles/issue-reviewer-dispatch.md`)が blocker_count(review_markdown の 🔴 件数を計数)・escalate(`scripts/evaluate-stop-condition.py` を呼んで補完)・marker(投稿コメント末尾の `<!-- issue-review: round=N has_blocker=B blocker_count=M -->`)を補完する。skill が返す任意 recommended_label は dispatch wrapper が捨てる(issue に適用しない・本契約に載せない)— ラベル同期は単一書込主体 orchestrator の専権で issue フェーズラベルは `need for human review` のみ(sink 時に orchestrator が付与)。issue 側には PR の `reaggregate-has-blocker.py` に相当する再集計 script は無い(多 source ゴム印対策は不要な単一 source のため blocker_count = 🔴 件数で足りる)。",
  "type": "object",
  "additionalProperties": false,
  "required": ["has_blocker", "blocker_count", "escalate", "review_markdown"],
  "properties": {
    "has_blocker": {
      "type": "boolean",
      "description": "🔴 の finding が 1 件以上あるか(`reviewing-github-issues` skill の出力。issue reviews は arch/google のような source 区別が無い単一 source のため、🔴 の有無がそのまま has_blocker)。orchestrator の outcome 解決(`clean_pass`/`blockers`)に使う。"
    },
    "blocker_count": {
      "type": "integer",
      "minimum": 0,
      "description": "blocker(🔴)件数。skill は blocker_count を返さないため、dispatch wrapper が review_markdown の 🔴 件数を計数して補完する(PR 側の `reaggregate-has-blocker.py` に相当する再集計 script は issue 側では新設しない)。停止条件判定(round_flag/trend_flag)の履歴推移(直近マーカーとの比較)に使う。"
    },
    "escalate": {
      "type": "boolean",
      "description": "停止条件(round_flag/trend_flag)による客観的エスカレーション(`scripts/evaluate-stop-condition.py` の出力。同 script は無改修で流用 — marker パーサが prefix 非依存のため issue-review マーカーをそのまま渡せる)。skill は escalate を produce しないため dispatch wrapper が評価器を呼んで補完する。true 時の orchestrator 側の遷移手順は `commands/harness-orchestrate.md`「ルーティング判定」節が唯一の正(ここでは複製しない)。escalate_to_human(主観的エスカレーション)とは独立のシグナル。"
    },
    "review_markdown": {
      "type": "string",
      "minLength": 1,
      "description": "`reviewing-github-issues` skill が生成したレビュー本文(dispatch wrapper が末尾に `<!-- issue-review: round=N ... -->` マーカーを埋め込み、`# Issue Reviewer - レビュー実施` を H1 として issue へ投稿する。投稿は dispatch wrapper の責務。この値は tick 報告用に返す)。findings 0 件の場合も空文字列ではなく本文(『findings 0 件 ✅』等)を返す。"
    },
    "escalate_to_human": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reason"],
      "description": "(任意)主観的エスカレーション(issue #31・`rules/escalate-to-human.md` 準拠)。issue reviewer は判定確定時の通常 4 フィールドに加えてこれを返してよい(排他ではなく共存可)。",
      "properties": {
        "reason": {
          "type": "string",
          "description": "人間の判断を仰ぎたい理由。空文字列不可の検証は `commands/harness-orchestrate.md`「主観的エスカレーション(issue #31・A案)」節の『最小の形式検証(A案)』が唯一の正(prose 側で行う。ここでは複製しない)。"
        }
      }
    }
  }
}
